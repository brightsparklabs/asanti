version: 2
updates:
- package-ecosystem: gradle
  directory: /
  schedule:
    # Defaults to first day of the month.
    interval: monthly
  open-pull-requests-limit: 20
  reviewers:
  - cameron-thomson-bsl
  labels:
  - dependencies
